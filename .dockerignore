# keep the build context small + reproducible
**/node_modules
**/dist
**/.vite
**/.env
**/.env.*
# only the server's runtime data (SQLite DB + confirmation PDFs) — NOT web/src/data,
# which is frontend source the Vite build needs. Don't broaden this back to **/data.
server/data
.git
.github
chats
project
README.md
*.local
**/.DS_Store
